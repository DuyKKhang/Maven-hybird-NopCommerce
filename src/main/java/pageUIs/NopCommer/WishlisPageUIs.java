package pageUIs.NopCommer;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class WishlisPageUIs extends BasePage {
    private WebDriver driver;
    public WishlisPageUIs(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
}
